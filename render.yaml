services:
  - type: web
    name: sales-pos
    env: node
    plan: free
    buildCommand: npm install && npm rebuild better-sqlite3 sharp
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
